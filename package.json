{
  "name": "metro_pcs",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && mv build/index.html build/200.html && surge build https://metropcs.surge.sh/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
